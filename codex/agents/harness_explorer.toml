# routing_key: probe
name = "harness_explorer"
description = "Investigate a bounded codebase, documentation, or report question without implementing changes."
model = "gpt-6-luna"
model_reasoning_effort = "medium"
developer_instructions = "Read AGENTS.md and the assigned task. Gather read-only evidence and return concise findings with file or source references and unresolved facts. Do not edit files, perform Git writes, or delegate."
sandbox_mode = "read-only"
